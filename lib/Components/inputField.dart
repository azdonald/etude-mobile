import 'package:flutter/material.dart';

class PasswordField extends StatelessWidget{
  final String hint;
  final bool obscure;

  PasswordField({this.hint, this.obscure});
  @override
  Widget build(BuildContext context) {
    
  }
}